#import '../fragment/meta_fragment.graphql'
#import '../fragment/category_fragment.graphql'

query GetCatefories($queryParams: QueryFilterDto!) {
  getCategories(queryParams: $queryParams) {
    meta {
      ...Meta
    }
    items {
      ...Category
    }
  }
}
